# Phase 1 — EDA Notes

To answer in writing before Phase 1 closes:

1. Eval-zone size per well — distribution of count and span of `TVT_input.isna()` rows.
2. GR comparability — per-well GR distributions; do means/scales differ enough to require per-well normalization?
3. Typewell TVT coverage — does the typewell's TVT range cover the lateral wells' TVT range?
4. MD step uniformity — is `MD` step constant within a well? Across wells?
5. Train/test well overlap — any wells in both? Geographic clustering?

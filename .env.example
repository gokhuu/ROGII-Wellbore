# Copy to .env and fill in. .env is gitignored.

# MLflow
MLFLOW_TRACKING_URI=./mlruns

# Kaggle (only if using the kaggle CLI to fetch data)
KAGGLE_USERNAME=
KAGGLE_KEY=

# Project paths (override if you store data elsewhere)
DATA_DIR=./data
